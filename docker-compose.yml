version: '3'
services:

  #Nginx Service
  # webserver:
  #   image: nginx
  #   container_name: webserver
  #   restart: unless-stopped
  #   tty: true
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   networks:
  #     - app-network

  web:
    image: nginx
    volumes:
      - ./templates:/etc/nginx/templates
    ports:
      - "8080:80"
    environment:
      - NGINX_HOST=foobar.com
      - NGINX_PORT=80

#Docker Networks
networks:
  app-network:
    driver: bridge
